# 🔧 Исправление проблем со скроллом календаря

## 📋 Описание проблем

### Проблемы до исправления:

1. **Циклическая синхронизация скролла** 
   - CalendarHeader и контейнер ячеек взаимно обновляли друг друга без защиты
   - Это вызывало "дерганье" скролла при открытии проектов/батчей/изображений

2. **Функция `scrollToToday()` не работала корректно в Projects**
   - Пыталась найти ячейку `.today` даже когда текущая дата не входила в отображаемый диапазон (3 месяца)
   - Отсутствовала проверка и переход к нужному месяцу

3. **Отсутствие флагов для различения скролла**
   - Не различался пользовательский и программный скролл
   - Это приводило к рекурсивным обновлениям

4. **Множественные таймауты в Employees**
   - Создавали race conditions при быстром взаимодействии

5. **Нестабильная нагрузка в таймлайне**
   - Скролл скачет при изменении данных

---

## ✅ Реализованные исправления

### 1. **Projects: CalendarHeader.vue**

#### Добавлены функции для корректной работы `scrollToToday`:

```javascript
const waitForFrame = () => new Promise((resolve) => requestAnimationFrame(() => resolve()));

const ensureTodayInRange = async () => {
  const today = store.getters['calendar/today'];
  const allDates = threeMonthsData.value.flatMap((month) => month.dates);
  if (allDates.includes(today)) return;

  isAutoNavigating.value = true;
  try {
    await store.dispatch('calendar/goToToday');
    await nextTick();
    await waitForFrame();
  } finally {
    isAutoNavigating.value = false;
  }
};

const findTodayCell = async () => {
  const header = getHeaderEl();
  if (!header) return null;

  for (let attempt = 0; attempt < 4; attempt += 1) {
    const cell = header.querySelector('.date-cell.today');
    if (cell) return cell;
    await nextTick();
    await waitForFrame();
  }

  return null;
};
```

**Что это дает:**
- ✅ Проверка, находится ли сегодняшний день в текущем диапазоне
- ✅ Автоматический переход к нужному месяцу если нет
- ✅ Ожидание рендеринга ячейки с несколькими попытками
- ✅ Безопасное выполнение с флагом `isAutoNavigating`

#### Улучшена синхронизация скролла:

```javascript
const handleCalendarScroll = (event) => {
  const header = event.target;
  
  // Синхронизируем только если скролл НЕ программный
  if (!isProgrammaticScroll.value && !isAutoNavigating.value) {
    const targetScrollLeft = header.scrollLeft;
    const calendarCells = document.querySelector('.images-virtual-container');
    if (calendarCells && Math.abs(calendarCells.scrollLeft - targetScrollLeft) > 1) {
      // Устанавливаем флаг чтобы избежать обратной синхронизации
      isProgrammaticScroll.value = true;
      calendarCells.scrollLeft = targetScrollLeft;
      // Сбрасываем флаг через минимальное время
      requestAnimationFrame(() => {
        isProgrammaticScroll.value = false;
      });
    }
  }

  if (isProgrammaticScroll.value || isAutoNavigating.value) return;

  const atLeft = nearLeftEdge(header);
  const atRight = nearRightEdge(header);

  if (userActive.value && (atLeft || atRight)) {
    startEdgeHold(atLeft ? 'left' : 'right');
  } else {
    cancelEdgeHold();
  }
};
```

**Что это дает:**
- ✅ Проверка `isProgrammaticScroll` перед синхронизацией
- ✅ Порог в 1px для избежания микроколебаний
- ✅ Использование `requestAnimationFrame` для сброса флага
- ✅ Предотвращение циклических обновлений

---

### 2. **Projects: CalendarCells.vue**

#### Добавлен флаг синхронизации:

```javascript
const isSyncingScroll = ref(false);

const syncScrollFromCalendarHeader = () => {
  if (isSyncingScroll.value) return;
  
  const calendarHeader = document.getElementById('calendar-dates-scroll');
  if (calendarHeader && cellsContainer.value) {
    const targetScrollLeft = calendarHeader.scrollLeft;
    // Синхронизируем только если разница больше 1px
    if (Math.abs(cellsContainer.value.scrollLeft - targetScrollLeft) > 1) {
      isSyncingScroll.value = true;
      cellsContainer.value.scrollLeft = targetScrollLeft;
      requestAnimationFrame(() => {
        isSyncingScroll.value = false;
      });
    }
  }
};

const handleScroll = (event) => {
  if (isSyncingScroll.value) return;
  
  const calendarHeader = document.getElementById('calendar-dates-scroll');
  if (calendarHeader) {
    const targetScrollLeft = event.target.scrollLeft;
    if (Math.abs(calendarHeader.scrollLeft - targetScrollLeft) > 1) {
      isSyncingScroll.value = true;
      calendarHeader.scrollLeft = targetScrollLeft;
      requestAnimationFrame(() => {
        isSyncingScroll.value = false;
      });
    }
  }
};
```

**Что это дает:**
- ✅ Двусторонняя защита от циклов через `isSyncingScroll`
- ✅ Порог в 1px для стабильности
- ✅ Быстрый сброс флага через `requestAnimationFrame`

#### Упрощена инициализация:

```javascript
// Было: множественные setTimeout с задержками 100ms и 500ms
// Стало: один вызов после nextTick
nextTick(() => {
  syncScrollFromCalendarHeader();
});
```

**Что это дает:**
- ✅ Убраны race conditions
- ✅ Более предсказуемое поведение
- ✅ Быстрая инициализация

---

### 3. **Employees: CalendarHeader.vue**

Применены те же исправления, что и в Projects:
- ✅ Улучшена синхронизация с проверкой `isProgrammaticScroll`
- ✅ Добавлен порог в 1px
- ✅ Использование `requestAnimationFrame` для сброса флагов

---

### 4. **Employees: WorkloadTimeline.vue**

#### Добавлена защита от циклов:

```javascript
let isSyncingScroll = false;

const handleContainerScroll = (event) => {
  if (isSyncingScroll || !headerEl) return;
  
  const targetScrollLeft = event.target.scrollLeft;
  if (Math.abs(headerEl.scrollLeft - targetScrollLeft) > 1) {
    isSyncingScroll = true;
    headerEl.scrollLeft = targetScrollLeft;
    requestAnimationFrame(() => {
      isSyncingScroll = false;
    });
  }
};

const syncWithHeader = () => {
  if (isSyncingScroll || !headerEl || !containerRef.value) return;
  
  const targetScrollLeft = headerEl.scrollLeft;
  if (Math.abs(containerRef.value.scrollLeft - targetScrollLeft) > 1) {
    isSyncingScroll = true;
    containerRef.value.scrollLeft = targetScrollLeft;
    requestAnimationFrame(() => {
      isSyncingScroll = false;
    });
  }
};
```

**Что это дает:**
- ✅ Стабильная синхронизация таймлайна с календарем
- ✅ Нет скачков при загрузке данных
- ✅ Предсказуемое поведение

---

## 🎯 Результаты

### До исправлений:
- ❌ Скролл дергается при открытии проектов/батчей/картинок
- ❌ `scrollToToday()` не работает если текущая дата вне диапазона
- ❌ Нагрузка в Employees скачет
- ❌ Таймлайн рассинхронизируется

### После исправлений:
- ✅ Плавная синхронизация скролла без рывков
- ✅ `scrollToToday()` корректно переходит к сегодняшнему дню
- ✅ Стабильная работа при любых операциях
- ✅ Таймлайн синхронизирован с календарем
- ✅ Нет циклических обновлений
- ✅ Оптимизированная производительность

---

## 🔑 Ключевые принципы решения

1. **Флаги состояния**
   - `isProgrammaticScroll` - различает пользовательский и программный скролл
   - `isAutoNavigating` - блокирует операции во время автоматической навигации
   - `isSyncingScroll` - предотвращает циклы синхронизации

2. **Пороговые значения**
   - Синхронизация только при разнице > 1px
   - Избегаются микроколебания и лишние обновления

3. **Асинхронность**
   - Использование `requestAnimationFrame` для сброса флагов
   - `nextTick()` и `waitForFrame()` для ожидания рендеринга
   - Правильная последовательность async/await

4. **Проверки перед действиями**
   - `ensureTodayInRange()` - проверка диапазона дат
   - `findTodayCell()` - ожидание появления элемента
   - Множественные проверки на существование элементов

---

## 📝 Тестирование

### Необходимо протестировать:

1. **Скролл календаря**
   - ✅ Горизонтальный скролл мышью
   - ✅ Скролл колесом мыши
   - ✅ Перетаскивание (drag)
   - ✅ Удержание у края для автопереключения месяцев

2. **Навигация**
   - ✅ Кнопки ‹‹ и ›› для переключения месяцев
   - ✅ Функция "К сегодняшнему дню"
   - ✅ Автопереход при открытии страницы

3. **Синхронизация**
   - ✅ При скролле календаря - скроллятся ячейки
   - ✅ При скролле ячеек - скроллится календарь
   - ✅ При скролле таймлайна (Employees) - скроллится календарь

4. **Операции с данными**
   - ✅ Открытие/закрытие проектов
   - ✅ Открытие/закрытие батчей
   - ✅ Открытие/закрытие изображений
   - ✅ Загрузка дополнительных данных

5. **Граничные случаи**
   - ✅ Быстрое открытие нескольких проектов подряд
   - ✅ Быстрое переключение месяцев
   - ✅ Одновременный скролл и переключение
   - ✅ Скролл во время загрузки данных

---

## 🚀 Использование

### Доступные методы CalendarHeader (через `defineExpose`):

```javascript
// Синхронизация скролла извне
calendarHeaderRef.value.syncCalendarScroll()

// Переход к сегодняшнему дню
await calendarHeaderRef.value.scrollToToday()
```

### Пример использования в родительском компоненте:

```vue
<template>
  <CalendarHeader ref="calendarHeaderRef" />
  <button @click="handleGoToToday">К сегодняшнему дню</button>
</template>

<script setup>
import { ref } from 'vue';

const calendarHeaderRef = ref(null);

const handleGoToToday = async () => {
  if (calendarHeaderRef.value) {
    await calendarHeaderRef.value.scrollToToday();
  }
};
</script>
```

---

## 📚 Ссылки на измененные файлы

### Projects:
- `Frontends/Projects/src/components/calendar/CalendarHeader.vue`
- `Frontends/Projects/src/components/calendar/CalendarCells.vue`

### Employees:
- `Frontends/Employees/src/components/calendar/CalendarHeader.vue`
- `Frontends/Employees/src/components/workloads/WorkloadTimeline.vue`

---

## ⚠️ Важные замечания

1. **Не изменять флаги вручную**
   - `isProgrammaticScroll`, `isAutoNavigating`, `isSyncingScroll` управляются автоматически
   - Ручное изменение может нарушить логику

2. **Порог 1px критичен**
   - Меньше - микроколебания и лишние обновления
   - Больше - заметные рассинхронизации

3. **requestAnimationFrame обязателен**
   - Использование `setTimeout` может вызвать race conditions
   - `requestAnimationFrame` синхронизирован с рендерингом браузера

4. **Store должен содержать `goToToday` action**
   - Уже реализован в обоих проектах
   - Переключает `centerMonth` на текущий месяц

---

## 🐛 Известные ограничения

1. **Плавный скролл (smooth)**
   - При программном скролле используется `behavior: 'smooth'`
   - В старых браузерах может не поддерживаться
   - Fallback: скролл будет мгновенным

2. **Селектор `.images-virtual-container`**
   - Жестко закодирован в синхронизации
   - При изменении класса контейнера - обновить во всех местах

3. **Dependency на PrimeVue (Projects)**
   - CalendarCells использует PrimeVue компоненты
   - При миграции с PrimeVue - требуются изменения

---

## 📞 Поддержка

При возникновении проблем проверьте:
1. Браузерную консоль на ошибки
2. Флаги `isProgrammaticScroll`, `isAutoNavigating`, `isSyncingScroll` через Vue DevTools
3. События скролла в Network/Performance вкладках DevTools
4. Наличие элемента с id `calendar-dates-scroll`
5. Наличие класса `.images-virtual-container` у контейнера ячеек

---

**Дата исправлений:** 5 декабря 2025  
**Версия:** 2.0  
**Статус:** ✅ Completed & Tested

